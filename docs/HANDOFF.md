# Session handoff — 2026-09-26

## Current state

Phases 0–3 are merged into main. Work is on `feat/phase-4-exercise-engine`; Phase 4 is implemented locally and remains under validation. Phase 5 has not started. GitHub Actions CI is preserved. The owner OAuth/session/profile implementation remains intact.

## Phase 4 implementation and pending gates

- Strict Zod discriminated definitions and answers support multiple choice, fill gap, typed answer, sentence reorder, matching, translation and error correction. Eight exercises are integrated into the existing five English lessons, alongside ten study/reflection blocks. Catalog schema, course and all lesson versions are intentionally 2.
- Public presentation is constructed through an allowlist. Correct IDs/orders/pairs, accepted forms, option feedback and explanations do not reach pre-submit HTML/client props. Server grading checks the owner, origin, profile language, current persisted lesson/version/activity and prerequisites before inserting an attempt. There is no submitted user ID or client-reported correctness.
- Attempts preserve the submitted answer and grading/feedback snapshot. The migration adds UUID identity, user/activity/version lookup, per-user submission-key deduplication and an update-denying trigger. Retrying creates a fresh record. Passing an exercise is not required for traversal, but a saved attempt is required; expected-position/version checks still prevent skips.
- Reusable controls use native radio, text and select inputs, plus token add/move/remove controls usable by keyboard and touch. Pending controls retain input, feedback receives focus, retry returns focus to the form, and saved input/feedback reload from the learner's last attempt. Text evaluation uses NFC, whitespace normalization and explicit case/final-punctuation policy, preserving accents and internal punctuation. Translation is authored Romanian→English, with no AI or equivalence claim.
- Local checks passed: 66 tests across 7 Vitest suites, lint, typecheck, formatting, clean migration generation, production build and audit (0 vulnerabilities). PGlite covered repeated migration/seed, immutable history, retries/replays, normalization and security boundaries. A regression reproduced JSONB key reordering breaking text transport replay; canonical schema parsing now makes equivalent replay objects compare correctly. An isolated PostgreSQL 17 Docker database also passed repeated migration/seed and catalog verification (18 blocks, 7 exercise types).
- All 13 production E2E tests passed on the isolated local PostgreSQL database using CI-only environment overrides, leaving `.env.local` unchanged. The temporary `phase4-e2e-*` owner fixture practised all seven types, saved 9 attempts (one incorrect), retried, recovered from an injected 503 without losing input, retained feedback across refresh, completed all five lessons and preserved progress after logout/new fixture session. Anonymous, forged-user, wrong-origin/type/activity/version and locked submissions were rejected. Axe, keyboard (including token moves) and overflow checks passed; desktop, 390 px, 320 px and light/dark captures were inspected. A genuine mobile defect was fixed: the sidebar had hidden Sign out together with its footer. Mobile account controls are now visible and exercised by E2E. Cleanup cascaded the fixture's sessions/progress/attempts. This is not a real owner GitHub callback test or a claim of manual owner exercise validation.
- With explicit owner authorization, migration `0004_unknown_amphibian.sql` and the Phase 4 seed were applied to the Neon development database and safely repeated. `db:inspect` ran before/after with certificate and hostname verification. The pre-migration Phase 4 `db:verify` failed as expected because `exercise_attempts` did not exist; a read-only snapshot confirmed the Phase 3 catalog (5 lessons, 10 blocks, 4 prerequisite links, lesson version 1). After migration/seed, `db:verify` passed: 1 course, 6 levels, 3 units, 5 lessons, 18 blocks, 7 exercise types, 4 prerequisite links, all lessons version 2, 0 attempts and 0 leftover test fixtures.
- SHA-256 comparisons of ordered whole-row snapshots confirmed unchanged existing data after both migration/seed passes: users 1, GitHub accounts 1, sessions 1, verifications 0, learner profiles 1 and lesson progress 1. No row data or credentials were displayed or written to snapshot files. The temporary read-only inspection script was removed. No database reset, truncation or recreation occurred. Existing version-1 progress is preserved; opening a revised lesson intentionally starts its version-2 progress.
- Branch `feat/phase-4-exercise-engine` is published to origin with [draft PR #3](https://github.com/NeacsuBogdan/lingua-studio/pull/3) targeting main. [GitHub Actions run #4](https://github.com/NeacsuBogdan/lingua-studio/actions/runs/36242958720) passed lint, typecheck, format, 66 unit/integration tests, clean migration generation, PostgreSQL migration/seed/catalog verification, production build, all 13 E2E tests and audit. No CI fixes were needed. The documentation update triggers the same full workflow again; require a green latest revision before review. No merge occurred.
- Remaining gate: the real owner must validate GitHub OAuth login/callback, all exercise controls and feedback, wrong-answer retry, refresh/navigation persistence, lesson completion/unlock, and logout/login persistence against Neon development. Automated signed-session fixtures do not establish this manual/live gate. Phase 4 is not complete; do not merge or start Phase 5.

## Development sign-in investigation (2026-09-25)

- A reported local `wss://127.0.0.1:3000/_next/hmr` error and apparently inactive sign-in button were investigated before merge. The owner restarted the development server and reported `[HMR] connected` and working sign-in. The original `wss://` state was not reproducible in a fresh direct HTTP run, so its exact cause remains unconfirmed. The installed Next.js 16.3.6 HMR client derives its socket protocol from the browser page protocol unless an absolute asset prefix overrides it; this project configures no asset prefix, HTTPS proxy or custom HMR URL. A direct `http://127.0.0.1:3000/sign-in` run opened `ws://127.0.0.1:3000/_next/hmr`.
- Fresh Playwright development inspection found the React click handler hydrated, no failed Next scripts or page errors, and a click sent `POST /api/auth/sign-in/social` with status 200 before navigating to GitHub authorization. In a restricted network sandbox, the same POST failed because access to Neon returned `EACCES`; that failure does not establish a bad OAuth credential or explain the owner's earlier browser state. With network access, the POST succeeded.
- A new production E2E regression test checks the click, Better Auth POST and GitHub handoff on the actual `127.0.0.1:3100` test origin. It intercepts GitHub before account authorization, so it cannot prove a live callback or owner session by itself. The previous Phase 2 live callback/session checks remain documented below. Recheck the owner's live callback and session before declaring the branch ready for merge.
- After the investigation, lint, typecheck, Prettier, all 26 Vitest tests, all 13 production E2E tests (including the temporary authenticated course fixture), production build and Neon connection check passed. The production sign-in test observed no HMR socket or client page error. No auth flow or secret values were changed.

## Phase 3 implementation

- The content model is language → course → CEFR level → unit → lesson → ordered activity blocks, with a prerequisite junction table and separate user/lesson progress. Languages now carry native names, writing direction and active status. The learner profile supplies the native→learning direction; English is the only active published course.
- The English catalog represents A1–C2. Three units and five lessons form a deliberate B1/B2 bridge; A1, A2, C1 and C2 are metadata only and are labelled as unpublished in the UI. The ten initial explanation/reflection blocks cover tense meaning, narrative sequence, collocations, polite requests and reading inference. These are study blocks, not graded Phase 4 exercises.
- `src/content/en/course.ts` is the versioned editorial source. `src/content/course-schema.ts` rejects invalid IDs, order, CEFR sequence, activity payloads/levels and bad or forward-pointing prerequisites. `seedEnglishCourse` publishes it transactionally with stable IDs and upserts, without modifying auth, profile or progress rows. Existing non-English language reference rows receive native-name metadata but remain inactive.
- `/course` reads persisted catalog/progress and shows level, unit, lesson, current position, recommended lesson and locked/unlocked/completed states. `/course/lesson/[lessonId]` renders persisted blocks, saves position and completion, and offers read-only review after completion. The dashboard now reports actual completed lessons instead of claiming zero sessions.
- Every lesson action calls `requireOwner`, validates route/position input and checks the selected active course. The repository checks prerequisites again on the server; a direct mutation cannot advance locked content. An expected-position comparison prevents skipped or replayed blocks. Progress is keyed by user and lesson, tagged with content version; starting a revised version resets that lesson's current progress rather than treating obsolete completion as current. There is no proficiency, score, streak or exam claim.

## Database and validation

- Migration `0003_daffy_giant_girl.sql` adds catalog/progress tables and additive language/course metadata columns. It passed PGlite before applying to Neon. `db:generate` reports no pending schema changes; repeat `db:migrate` succeeds. Repeating `db:seed` leaves the catalog counts unchanged, and `db:verify` reports 1 English course, 6 levels, 3 units, 5 lessons, 10 blocks, 4 prerequisite links and 1 existing profile. No real owner progress was created by tests.
- Local checks passed: lint, typecheck, Prettier, 26 Vitest tests across 5 suites (including migration, seed, ordering, unlock, persistence, user isolation and content-version restart), production build, Neon connection/migration/seed verification and npm audit (0 vulnerabilities).
- Playwright passed 11 tests against the production build. The authenticated course test creates a temporary verified test user, linked GitHub account and signed PostgreSQL session using a process-only test owner ID. It submits the start action from an anonymous browser and confirms denial, completes the first lesson with the signed session, checks refresh persistence and the next unlock, verifies locked routes, then deletes the fixture user (including its session/progress through FK cascades). The pre-existing owner account/profile is not used or changed. Phase 2 separately validated the real GitHub OAuth callback with the owner's account.
- Real browser captures of the course map and lesson were inspected at desktop, 390 px and 320 px in light/dark themes. Axe WCAG 2/2.1 A/AA checks, keyboard focus, no page errors and no horizontal overflow passed. A low-contrast locked-row style was found and corrected during this validation.

## Next session

Continue only Phase 4 on its current feature branch until the outstanding gates pass. Do not start Phase 5 or merge main. Increment `contentVersion` when lesson semantics change, validate the catalog and apply reviewed migrations before reseeding. Do not delete content/progress/attempt rows casually; removed published activity IDs require an explicit migration. `.env.local` stays ignored and must never be committed.
